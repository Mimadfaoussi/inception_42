services:
  mariadb:
    container_name: mariadb
    build:
      context: ./requirements/mariadb
    env_file:
      - .env
    volumes:
      - wordpress_db:/var/lib/mysql
    ports:
      - "3306:3306"
    networks:
      - inception
    restart: always 

  nginx:
    container_name: nginx
    build:
      context: ./requirements/nginx
    ports:
      - "443:443"
    extra_hosts:
      - "mfaoussi.42.fr:127.0.0.1"
    volumes:
      - wordpress_files:/var/www/html
    networks:
      - inception
    restart: always

  wordpress:
    container_name: wordpress
    build:
      context: ./requirements/wordpress
    env_file:
      - .env
    volumes:
      - wordpress_files:/var/www/html
    networks:
      - inception
    restart: always

networks:
  inception:
    driver: bridge

volumes:
  wordpress_db:
    driver: local
  wordpress_files:
    driver: local
